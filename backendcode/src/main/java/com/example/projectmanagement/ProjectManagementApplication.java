package com.example.projectmanagement;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.data.jpa.repository.config.EnableJpaAuditing;

//@SpringBootApplication // Keep only one @SpringBootApplication in EmSsystemApplication
//@EnableJpaAuditing // Keep only one @EnableJpaAuditing in EmSsystemApplication
public class ProjectManagementApplication {

    public static void main(String[] args) {
        // This main method is likely unused now as EmSsystemApplication is the entry point
        // SpringApplication.run(ProjectManagementApplication.class, args);
    }
} 